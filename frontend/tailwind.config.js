/**
 * TailwindCSS отключен, так как текущий интерфейс использует кастомные стили.
 * Конфигурация оставлена пустой, чтобы build-пайплайн CRA не пытался
 * подключать отсутствующие плагины.
 */
module.exports = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};