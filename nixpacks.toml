[phases.setup]
nixPkgs = ["nodejs", "npm", "python312"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd frontend && npm install --legacy-peer-deps"
]

[phases.build]
cmds = [
  "cd frontend && npm run build"
]

[start]
cmd = "python startup.py"
